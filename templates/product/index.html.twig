{% extends 'base.html.twig' %}

{% block title %}Products list{% endblock %}

{% block body %}
    {{ parent() }}
    <h2 class="header-page">Lista produktów</h2>
    <a href="{{ path('app_admin_product_new') }}">Dodaj nowy produkt</a>
    {% for product in products %}
        {{ include('product/_templates/product.html.twig') }}
    {% endfor %}
    {{ include('product/_templates/links_in_list.html.twig') }}
{% endblock %}
